apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: map-ingress
spec:
  rules:
    - host: signup.yourdomain.com
      http:
        paths:
          - path: /map
            pathType: Prefix
            backend:
              service:
                name: map-service
                port:
                  number: 80
